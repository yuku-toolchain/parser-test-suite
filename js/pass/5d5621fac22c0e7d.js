function hasOwnProperty(obj, property) {
  return Object.prototype.hasOwnProperty.call(obj, property);
}
import('./get-nested-namespace-props-nrml-1_FIXTURE.js').then(ns => {}).then($DONE, $DONE).catch($DONE);