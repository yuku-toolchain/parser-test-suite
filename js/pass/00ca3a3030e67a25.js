const maxYMW = Math.pow(2, 32) - 1;
const maxDays = Math.floor(Number.MAX_SAFE_INTEGER / 86400);
const maxHours = Math.floor(Number.MAX_SAFE_INTEGER / 3600);
const maxMinutes = Math.floor(Number.MAX_SAFE_INTEGER / 60);
const maxSecs = Number.MAX_SAFE_INTEGER;
const balanceMaxSecondsMilliseconds = new Temporal.Duration(0, 0, 0, 0, 0, 0, maxSecs - Math.floor(maxSecs / 1000), maxSecs, 0, 0);
const balanceMinSecondsMilliseconds = new Temporal.Duration(0, 0, 0, 0, 0, 0, -(maxSecs - Math.floor(maxSecs / 1000)), -maxSecs, 0, 0);
const balanceMaxSecondsMicroseconds = new Temporal.Duration(0, 0, 0, 0, 0, 0, maxSecs - Math.floor(maxSecs / 1000000), 0, maxSecs, 0);
const balanceMinSecondsMicroseconds = new Temporal.Duration(0, 0, 0, 0, 0, 0, -(maxSecs - Math.floor(maxSecs / 1000000)), 0, -maxSecs, 0);
const balanceMaxSecondsNanoseconds = new Temporal.Duration(0, 0, 0, 0, 0, 0, maxSecs - Math.floor(maxSecs / 1000000000), 0, 0, maxSecs);
const balanceMinSecondsNanoseconds = new Temporal.Duration(0, 0, 0, 0, 0, 0, -(maxSecs - Math.floor(maxSecs / 1000000000)), 0, 0, -maxSecs);